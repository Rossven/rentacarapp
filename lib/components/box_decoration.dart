import 'package:car_rent/components/colors.dart';
import 'package:flutter/material.dart';

BoxDecoration kBoxDecoration = BoxDecoration(
  color: greyColor,
  border: Border.all(
    color: greyColor,
    width: 5,
  ),
  borderRadius: BorderRadius.circular(10),
);

BoxDecoration kLocBoxDecoration = BoxDecoration(
  color: greyColor,
  border: Border.all(
    color: greyColor,
    width: 5,
  ),
  borderRadius: BorderRadius.circular(20),
);
